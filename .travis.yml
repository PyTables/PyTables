language: python

python:
  - 2.6
  - 2.7
  - 3.2
  - 3.3
  - 3.4

sudo: false

addons:
  apt_packages:
    - libhdf5-serial-dev
    - liblzo2-dev

install:
  - pip install -U -r requirements.txt
  - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip install -U unittest2; fi

script: make check
