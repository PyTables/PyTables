name: CI

on: [push, pull_request]

jobs:
  build:
    name: ${{ matrix.os }} ${{ matrix.python }} ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ['3.6', '3.7', '3.8', '3.9', '3.10']

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python }}
      uses: conda-incubator/setup-miniconda@v2
      with:
        python-version: ${{ matrix.python }}
        auto-update-conda: true
        channels: conda-forge
        channel-priority: strict
    - name: Install dependencies
      run: |
        conda install setuptools pip wheel numpy numexpr cython bzip2 hdf5 lzo
        # conda install sphinx sphinx_rtd_theme numpydoc ipython
    - name: Source distribution
      run: |
        python setup.py sdist
    - name: Installation
      run: |
        pip install -v dist/*.tar.gz
    - name: 'Run test'
      run: |
        cd .. && python -m tables.tests.test_all
        pt2to3 -h
        ptrepack -h
        ptdump -h
        pttree -h

