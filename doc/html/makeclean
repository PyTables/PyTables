# This is a generated file. Do not edit it.
# If you call this as a batch file, it removes all
# files generated by tbook.

# First, everything with index creation
rm -f usersguide-html-ind.xml
rm -f usersguide-html-idx.xml
rm -f usersguide-html.idx

# Secondly, the general image creation
rm -f usersguide-html-image-sizes.xml
rm -f usersguide-html-image-gallery-raw.tex
rm -f usersguide-html-image-gallery.tex
rm -f usersguide-html-image-gallery.dvi
rm -f usersguide-html-image-gallery.log

# Thirdly, the EPS creation
rm -f objecttree-h5.eps
rm -f tutorial-h5.eps
rm -f tutorial2-h5.eps

# Forthly, the PDF creation
rm -f objecttree.pdf

# Fifthly, the HTML image creation
rm -f objecttree-h5-web.jpg
rm -f objecttree-web.png
rm -f tutorial-h5-web.jpg
rm -f tutorial2-h5-web.jpg

# Sixthly, the bibliography files
rm -f usersguide-html-bib.aux
rm -f usersguide-html-bib.blg
rm -f usersguide-html-bib.xml

# Seventhly, the HTML png comments
rm -f objecttree-image-comment.txt

# Next, the HTML image dimensions
rm -f objecttree-h5-image-size.txt
rm -f objecttree-image-size.txt
rm -f tutorial-h5-image-size.txt
rm -f tutorial2-h5-image-size.txt
