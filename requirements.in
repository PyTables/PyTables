# Runtime dependencies for ``pip install``.
# Keep list in sync with ``project.dependencies`` in ``pyproject.toml``.

blosc2>=2.3.0
numexpr>=2.6.2
numpy>=1.25
packaging
py-cpuinfo
typing-extensions>=4.4.0  # workaround for anyio
